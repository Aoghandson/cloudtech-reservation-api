version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "handson12"
        LoadBalancerInfo:
          ContainerName: "handson"
          ContainerPort: 80